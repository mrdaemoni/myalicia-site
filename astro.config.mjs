import { defineConfig } from 'astro/config';

// myalicia.com — Astro config.
// Static site, deployable anywhere (Netlify, Vercel, GitHub Pages, Cloudflare Pages).
export default defineConfig({
  site: 'https://myalicia.com',
  output: 'static',
});
